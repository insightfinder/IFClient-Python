from ifclient.config.models.v1.tool_config import ToolConfigV1
from typing import Dict, Any

def config_merge(toolConfig: ToolConfigV1) -> Dict[str, Any]:

    try:
        
        pass

    except Exception as e:
        print("An exception occured while merging the configurations")
        raise e
